bin/
dist/

# `just dev` and `make dev`: config, state, and lake for development runs.
/.dev/

# `go build ./cmd/terva-lampi` with no -o drops the binary at the repo root.
/terva-lampi
/lampi

*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.txt
profile.out

go.work
go.work.sum

.env
.env.local
.env.*.local

.DS_Store
*.swp
*.swo
*~

# Decision trails from show-me-your-work, uncommitted while in flight
.audit/
decisions.tsv
